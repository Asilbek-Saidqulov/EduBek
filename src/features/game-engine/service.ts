/** Game Engine service — composes all 18 systems. */
export { createMatch, getMatch, listMatches, destroyMatch, updateMatchState, updateMatchStatistics, validateTransition, attemptTransition, getValidTransitions, VALID_TRANSITIONS } from "./match-engine";
export { lobbyJoin, lobbyLeave, lobbyKick, lobbyBan, lobbyLock, lobbyTransferHost, lobbyAssignTeam, createSession, getSession, updateSessionStatus, updateHeartbeat, checkTimeouts, setPlayerReady, getReadyCheckState, teacherOverrideReady, startRound, finishRound, advanceQuestion } from "./lobby-session-round";
export { preloadQuestion, advanceQuestionPhase, recordAnswer, startTimer, getTimer, pauseTimer, resumeTimer, extendTimer, syncTimer, nextSequenceNumber, createSyncSnapshot, recordSyncEvent, getSyncReport, validateSyncEvent, emitEvent, getEvents, subscribe, clearEvents } from "./pipeline-sync-events";
export { receiveScoreEvent, validateScoreEvent, normalizeScoreEvent, getScorePipelineReport, registerResource, processResourceAction, getResourceBalance, getResourceHistory, saveReplay, getReplay, createReplayState, stepReplay, addSpectator, removeSpectator, getSpectators, recoverPlayer, detectCheat, getCheatFindings, checkDuplicateSubmission, checkImpossibleTimestamp, recordMatch, getMatchRecord, generateGameAnalytics } from "./pipelines-replay-security";
